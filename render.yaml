services:
  - type: web
    name: danceplayer
    runtime: static
    branch: main
    buildCommand: npm run build
    staticPublishPath: dist
    autoDeployTrigger: commit
    routes:
      - type: rewrite
        source: /*
        destination: /index.html